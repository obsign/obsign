# Rust build artifacts
/target

# macOS
.DS_Store

# Local demo output (evidence packs, WAL, tokens) — never commit these:
# they can contain real subjects, real issuers and real signing seeds.
/tmp-demo
*.jwt
.playwright-mcp/

# Agent scratch space. Ignored here rather than only in .git/info/exclude, so
# the rule travels with the clone instead of living on one machine.
.context/

[package]
name = "pkcs11"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Minimal hand-rolled PKCS#11 signer, shared by the ledger sealer and the gateway identity key."

[dependencies]
hex = { workspace = true }
libc = { workspace = true }
thiserror = { workspace = true }
